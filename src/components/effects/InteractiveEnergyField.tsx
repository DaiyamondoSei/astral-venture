
import React from 'react';
import InteractiveEnergyField from './energy-field/InteractiveEnergyField';

// Re-export the component with the same interface to avoid breaking existing code
export default InteractiveEnergyField;
